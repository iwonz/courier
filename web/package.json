{
  "name": "courier-web",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "ui",
    "data",
    "admin",
    "landing"
  ],
  "scripts": {
    "browser:test": "../scripts/test-browser.sh",
    "build": "npm run build --workspace @courier/ui && npm run build --workspace @courier/data && npm run build --workspace @courier/admin && npm run build --workspace @courier/landing",
    "check:assets": "npm run check:assets --workspace @courier/ui",
    "test": "npm run test --workspace @courier/ui && npm run test --workspace @courier/data && npm run test --workspace @courier/admin && npm run test --workspace @courier/landing",
    "verify": "npm run check:assets && npm run test && npm run build && npm run check:embed --workspace @courier/data && npm run check:embed --workspace @courier/admin"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "@tailwindcss/vite": "4.3.3",
    "@testing-library/react": "16.3.3",
    "@testing-library/user-event": "14.6.7",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "@vitejs/plugin-react": "6.1.1",
    "tailwindcss": "4.3.3"
  }
}
