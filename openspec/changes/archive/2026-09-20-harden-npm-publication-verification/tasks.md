## 1. Publication verification

- [x] 1.1 Extend npm visibility polling to six minutes with online revalidation and verify workflow syntax remains valid
- [x] 1.2 Document the bounded asynchronous-processing behavior and verify the release runbook matches the workflow

## 2. Release readiness

- [x] 2.1 Validate the OpenSpec change strictly and run the full repository verification gate
- [x] 2.2 Verify the workflow preserves exact package/version diagnostics and the documented one-command release handoff
