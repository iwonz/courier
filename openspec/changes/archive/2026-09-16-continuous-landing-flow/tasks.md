## 1. Shared UI primitives

- [x] 1.1 Replace the command-demo component with the immutable command-readout API and verify copy success, failure, selection reset, slots, and removal of timers/transcript exports in unit tests
- [x] 1.2 Add shared control-frame tokens and the semantic icon-link component, then verify link security, accessible naming, theme behavior, and segmented-control token reuse in unit tests
- [x] 1.3 Add one-shot eager/proximity/native-lazy scene activation and on-demand refraction cleanup, then verify observer, media-query, pointer, animation-frame, and disconnect paths in unit tests

## 2. Continuous landing

- [x] 2.1 Remove tagline and simulation state/copy from both catalogs and landing rendering, retain selectors and Copy behavior, and verify the generated route, installation, and CLI commands remain immutable and usable
- [x] 2.2 Replace scroll snapping and fixed slides with natural responsive section geometry, measured-header anchor offsets, reduced-motion navigation, and reading-band active tracking, then verify layout and navigation unit tests
- [x] 2.3 Apply shared icon-link chrome to GitHub and use the command readout for all landing command surfaces, then verify external-link protection, localized live copy status, and absence of editable/demo controls
- [x] 2.4 Add panorama overlap, content visibility, stable intrinsic placeholders, responsive sources, and deterministic JavaScript/CSS budget checks, then verify production bundle limits

## 3. Responsive artwork

- [x] 3.1 Generate and visually inspect temporary wide and portrait continuous Relay journey storyboards using the repository mascot as identity reference
- [x] 3.2 Generate, optimize, and inspect eight final WebP panorama segments for continuity, responsive composition, prohibited content, and the 225 KiB per-file and 1.6 MiB aggregate budgets
- [x] 3.3 Update provenance with prompts, roles, dimensions, bytes, hashes, and sequence metadata, remove superseded landing assets and temporary generation artifacts, and verify the asset gate

## 4. Documentation and acceptance

- [x] 4.1 Update English brand, landing, UI, and acceptance documentation and verify obsolete simulation and snap behavior is absent
- [x] 4.2 Maintain exact TypeScript and Go coverage and expand browser acceptance for natural scrolling, responsive loading order, panorama continuity, themes, locales, motion, input modes, Copy behavior, delivery isolation, and administration controls
- [x] 4.3 Run asset and bundle gates, `make verify`, strict OpenSpec validation, Pages build, browser acceptance, GoReleaser Community snapshot, and clean-worktree verification
- [x] 4.4 Archive the completed OpenSpec change into baseline specifications, create the single conventional commit, push the feature branch, fast-forward `main`, monitor CI and Pages, smoke-test desktop/mobile production, and remove temporary browser artifacts
