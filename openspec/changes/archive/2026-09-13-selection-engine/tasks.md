## 1. Shared policy

- [x] 1.1 Compile ordered gitignore, regex, and rule-file occurrences
- [x] 1.2 Normalize root-relative paths and preserve gitignore negation behavior
- [x] 1.3 Fail invalid rules and unreadable rule files before endpoint acquisition

## 2. Consumers

- [x] 2.1 Apply the selector to transfer scan and copy walks
- [x] 2.2 Apply the same selector to archive creation
- [x] 2.3 Ship the three selection flags and update generated command documentation

## 3. Quality

- [x] 3.1 Cover ordering, negation, regex, Unicode, directories, rule files, and all failure paths
- [x] 3.2 Run strict OpenSpec validation and the full verification gate
